# Agri.in acceptance checklist (AG rows)

Started at A-U1 CP1 per the build prompt §5. **Rows are appended by later
checkpoints/passes, never rewritten.** A row goes ✅ only with evidence — a
binding proof row in `docs/design-reference/polish-a1.md`, a spec file, or a
recorded run — never by assertion. Launch gate (D57): every row green in a
live browser.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A1 | Guest home renders with a clean console (zero errors/warnings) — milk's A1 failure not repeated | Live browser as guest at 360/1280, DevTools console capture archived; e2e spec asserts no `console.error` on load | ✅ **the spec now runs in CI and passes** — 3 consecutive green passes 2026-08-18. It was never the evidence that was missing: `agri-home.spec.ts` asserts zero console errors and zero 401s on guest navigation and has done since A-U1. It ran in NO job (no `@matrix` tag, absent from `e2e:auth`), so the condition this row set itself could not arrive. Wired in via `pnpm run e2e:agri`. |
| AG-A2 | Category grid renders exactly the registry — tile count = registry entry count, zero hardcoded category lists | e2e spec: fetch `/catalog/verticals`, assert DOM tile count equals registry count; code review: no literal category arrays in app code | ✅ **verified by a run that now happens.** `e2e/agri-categories.spec.ts` asserts the DOM tile count equals a live cursor-walk of `GET /catalog/verticals` — 12/12 green 2026-08-18. The number is **39**, not 36: A-U2's migration 0042 added fisheries, nurseries and poultry. The spec had said 36 since A-U1 and nobody noticed, because **nothing ran it** — it carries no `@matrix` tag and was not in `e2e:auth`'s name list, so neither CI job touched it. Now wired in as `pnpm run e2e:agri` inside the e2e-auth job. A spec nobody runs is documentation, not a gate. |
| AG-A3 | Soon landing is self-noindexed and notify-me round-trips to a real subscription | e2e: assert `noindex` meta on landing; submit notify-me, assert 2xx + row via API; proof row records which module (pincode-interest vs notify) | ✅ **verified in CI** — `agri-categories.spec.ts` asserts the `noindex` meta on `/c/seeds` and drives the real notify-me round-trip (641001 → pincode-interest through the live BFF). Green across 3 consecutive passes 2026-08-18. The "CP3 e2e batch" it pointed at existed all along but ran in no CI job; that is what changed. |
| AG-A4 | `agri_home_hero_xl` ad slot serves approved house creatives, respects frequency caps, always labelled "Ad", zero CLS | e2e with cap reset before repeated-load (`ads_freq_cap_per_day = 3`); Lighthouse CLS on `/`; visual check of the Ad tag; config-only diff (no engine code edits) | ⬜ pending (CP2) |
| AG-A5 | Search band submits to `/search` and returns results; location chip reflects `/identity/location` | e2e: type query → results page renders hits; binding proof row with the real API call | ⬜ pending (CP2) |
| AG-A6 | Directory row shows REAL businesses for the pincode; sponsored card only when a real campaign exists, else organic-only | Binding proof row (screenshot + `/directory` call); e2e against seeded dev data asserts organic-only when no campaign | ⬜ pending (CP2) |
| AG-A7 | Locale sweep: EN/TA/HI render every home/categories string via next-intl; no hardcoded copy; Tamil strings flagged where they read wrong | e2e: one browser context per locale (NEXT_LOCALE trap); screenshot set 360/390/768/1280 × EN/TA/HI archived | ✅ **CI green** — the per-locale contexts (ta / hi / en, one browser context each for the NEXT_LOCALE trap) pass in `agri-home.spec.ts`, now actually executed in CI, 3 consecutive passes 2026-08-18; 24-shot matrix archived in a1/. **Native TA/HI review is still outstanding and is not something a spec can discharge** — the tests prove strings resolve through next-intl, not that a Tamil farmer would call the wording right. |
| AG-A8 | Lighthouse ≥ 0.90 (throttled-3G) on `/` and `/categories` — no carve-out, from PR one | LHCI in CI, median of 5 runs each, with `agri_today` ON and REAL data behind it (live Open-Meteo, ingested Agmarknet, E5 datasets): `/` perf **0.95** a11y **1.00**, `/categories` perf **0.96** a11y **0.98** (run 31999694407, lighthouse green). No carve-out taken. Note the a11y numbers only became honest at A-U2: the flag-off home Lighthouse used to audit did not contain the weather strip or schemes bar, so their contrast/target-size defects went unmeasured until the flip exposed and fixed them | ✅ verified |
| AG-A9 | Reduced-motion sweep: every animation static with content fully visible (sparklines drawn, tiles opacity 1, marquee static, count-up shows finals) | e2e with `prefers-reduced-motion: reduce` context: assert sparkline visible, tile opacity 1, stat values final; spec per §4 of the build prompt | ✅ **CI green** (3 consecutive passes 2026-08-18, now that the spec is actually run): dashoffset 0, tiles opacity 1, marquee static; NOTE: reveal/count-up motion deferred on the home under the AG-A8 floor (polish-a1 §0) — static state = the rm fallback, so this row's guarantees hold trivially there; flips ✅ on CI green |
| AG-A10 | Production build without `AUTH_SESSION_SECRET` renders the guest home — no secret → guest, never 500 | Spec: build + boot without the secret, assert 200 + guest header (milk §2b lesson, agri-explicit) | ⬜ pending (CP2/CP3 spec). **2026-08-20 (A-U4b C3): ✅ re-run live and CLEAN.** Production `next build` + `next start` with `AUTH_SESSION_SECRET` empty: `/` renders the guest home with the Login pill, **zero console messages, zero non-2xx requests** (capture: `docs/qa/evidence/a-u4b/c3-secretless-prod-clean-console.png`). The first run of this check found three console 500s hiding behind the green page — the identity proxy and /api/auth/* threw on the lazy secret guard — fixed by degrading the guest-capable proxy to ‘no token’, answering /api/auth/* with a typed 503 (401 for /me, 200 reachable:false for the silent probe), while the server log still names the missing env var per request. The automated prod-build harness this row asks for is still an open want; the live re-run is the evidence today |
| AG-A11 | Sarkari hub links resolve to OFFICIAL domains only; `verified_against`/`verified_on` stamps render from data; link-checker green ≤ 7 days before launch | `scripts` link-checker run recorded; spec asserts stamp text comes from the dataset, not literals; domain allowlist review | ✅ **re-run 2026-08-18: 6/6 OK, exit 0** (pmKisan, patta, pmfby, agristack, shc, enam — all https, all under declared gov.in/nic.in domains). One caveat kept honest: the first run showed pmKisan `UND_ERR_CONNECT_TIMEOUT`, and a retry resolved it 200 in 0.3s across three samples — the transient flake this checker is deliberately not wired into CI for. **Still expires:** re-run ≤7 days before launch (D57 gate), because this row is about the links being alive that week, not this week. |
| AG-A12 | Farm calculators (EMI · seed rate · fertilizer dose from SHC · spray dilution) compute correctly and work offline | Unit tests per formula with hand-checked fixtures; e2e offline-mode run of `/tools`; formulas' sources cited in code | ✅ **PROVEN OFFLINE — and the old evidence did not support the claim.** `node scripts/verify-offline-tools.mjs` on a production build: 100000 @ 11% over 60m → **₹2,174 with the network off**. What was here before was 12 unit tests (the arithmetic) and a spec asserting zero fetch/xhr during compute (network-*purity*) — neither of which is *works offline*, and `/tools` was in neither `PRECACHE` nor `RUNTIME_CACHEABLE`, so offline it fell through to the shell and the calculators could not be reached at all. Both proofs stayed green throughout. The obvious fix was worse: precaching served the HTML with no JS (chunks live under `/_next/static`, deliberately uncached), so every field sat at its default and the answer read ₹11,649 whatever you typed — a calculator that looks alive and is not. `/tools` is therefore RUNTIME-cached, tying the copy to a real visit, which is also what puts its JS in the HTTP cache. Write-up: `docs/qa/agri-offline-tools.md`. |
| AG-A13 | Grid renders exactly 36 registry verticals — `farm-tools` live, `machinery-rental` Soon (· CHC) | e2e: tile count === 36 asserted against the registry response; registry diff shows the two new entries added this pass | ✅ **verified — 39, not 36.** 0037 seeded 36; A-U2's 0042 added fisheries, nurseries and poultry. The exact-count tripwire is kept deliberately (the DOM-vs-registry assertion passes happily if the registry silently empties, and this line is the only thing that would notice) — but it must be updated WITH the migration that moves it, naming the slugs. It went stale for two passes because no CI job ran the spec; fixed — see AG-A2. |

| AG-A14 | Weather renders REAL per-pincode data with source + as-of visible | Live dev capture: `/market/today/641001` → Open-Meteo values rendered on `/` (26.7 °C, 72 %, 22 km/h SW, soil 25.5 °C at 08:48 IST 2026-08-16); 16 unit tests drive the parser from a captured LIVE response; `weather.source` asserted non-empty and rendered verbatim | ✅ verified — live capture + 16 tests; CI run 31989984411, all 11 jobs green |
| AG-A15 | Mandi cards match raw ingested rows (spot-check 3 commodities against the API) | Real Tamil Nadu pull with the registered key, 2026-08-17: fetched 5776, written 601, quarantined 0, 5175 skipped as uncurated. Spot-checked 3 commodities against the raw API, min/max/modal each — Onion Hosur 4500/5000/4750 and Krishnagiri 4000/4500/4250; Tomato Perundurai 2200/2500/2350 and Thuraiyur 2400/2700/2550; Banana Viruthachalam 4000/7000/5500 and Ranipettai 10000/10000/10000 — ALL EXACT. The single conversion holds: tomato 2400/qtl renders ₹24.00/kg. Home serves 6 real commodities for Coimbatore with `as_of 2026-08-17` and `stub: false` | ✅ verified |
| AG-A16 | Price-alert subscription round-trips and fires after a pull | BUILT (0044). The A-U1 card's CTA now POSTs to `/market/alerts` through the same-origin BFF (bearer never touches JS) and the daily pull calls `dispatch_due_alerts` after the ingest COMMITS, publishing `market.price_alert` on the `market` stream which notify consumes (`mandi_price_alert`, in_app, en/ta/hi). Keyed on (user, pincode) — the card asks for an AREA, and the digest covers whatever curated commodities reported there. 8 tests: idempotent subscribe, per-user cap, IDOR (another user's id 404s exactly like a nonexistent one), once-a-day latch across a re-run pull, silence when there is nothing to say, and the stream/route wiring. Verified live: GET+POST `/market/alerts` 401 unauthenticated | 🟡 **backend half genuinely covered; the browser round-trip is NOT, and is now an open question rather than a gap.** `tests/test_market_alerts.py` (in CI) covers the subscription and `dispatch_due_alerts` firing after an ingest. The logged-in e2e was written and is committed as `test.fixme`: the click leaves **no request at `/market/alerts` in the API access log at all**, so the POST never leaves the browser, and the card's disabled state proved nothing because it disables while *saving* too. Cause not established — BFF, session, or the test racing silent SSO. Recorded rather than quietly dropped. **2026-08-20 (A-U4b C2): ✅ resolved — the app was never broken; the SPEC raced its own POST.** The card disables synchronously on "saving", so `toBeDisabled()` was satisfied while the POST was still in flight (the BFF route compiles on its first dev hit — whole seconds); the spec then read the list before its own POST landed, and teardown aborted the request, which is why the API access log never recorded it (browser trace: POST, response status −1). Both login paths (interactive redirect AND silent-SSO-minted sessions) verified live to land a 201 through the BFF. `test.fixme` removed; the spec now synchronizes on `waitForResponse` and asserts the 2xx; green 3/3 locally, runs in CI via `e2e:agri` |
| AG-A17 | MSP overlay values match the verified dataset | Seeded from the PRIMARY source (0043): "Cabinet approves MSP for Kharif Crops for Marketing Season 2026-27", PIB, released 13 May 2026 — Paddy (Common) ₹2441/qtl and Groundnut ₹7517/qtl, each cross-checked against the release's own year-on-year delta (2441−2369=72 ✓, 7517−7263=254 ✓). Renders as MSP ₹24.41 and ₹75.17 beside live prices of ₹24.0 and ₹75.0. Only mandated crops get a row — tomato/onion/banana/turmeric/dry-chilli and coconut (copra is the mandated commodity, not coconut) carry NO note. Transcribed by the agent, not a human: owner should spot-check both figures against the linked release | ✅ **independently re-fetched from the primary source 2026-08-18.** PIB release PRID=2260618 pulled live (HTTP 200, 281 KB) and read: **"Paddy Common 2441"** and **"Groundnut 7517"** — exactly the seeded values, so the transcription in 0043 is confirmed against the source rather than trusted. NOTE on what this is worth: a second machine read of the same page is not the same thing as a human checking, so an owner glance is still welcome — but the specific risk this row named (an agent mis-transcribing a number) is now addressed with evidence. |
| AG-A18 | Compare view rows match the underlying table | `/mandi/[commodity]` renders a per-market compare table from `/market/commodities/{slug}`; verified live with two markets (Coimbatore ₹24.0 / Pollachi ₹23.6, each with its OWN series and as-of) and unit-tested that one market's series never absorbs another's | ✅ verified — live capture + 7 tests; CI run 31989984411, all 11 jobs green |
| AG-A19 | Outage degradation shows a stale stamp (kill the worker, reload) | Test `test_outage_with_a_warm_cache_serves_stale_with_a_visible_stamp`: upstream raises, payload still 200 and `weather.source` starts `Open-Meteo · as of …`; cold-cache twin asserts 503 (nothing invented) | ✅ verified — CI run 31989984411, all 11 jobs green (backend + e2e-auth + e2e-matrix) |
| AG-A20 | Zero stub/fixture code remains (grep for the fixture markers) | `market_data/fixtures.py` DELETED; `stub` pinned False in the payload and asserted so; e2e asserts the page contains no `(stub)` text anywhere; `grep -rn 'today_fixture\|market_data.fixtures' backend/core` returns nothing | ✅ verified — e2e asserts no "(stub)" text anywhere; CI run 31989984411, all 11 jobs green |
| AG-A21 | Lighthouse still ≥ 0.90 on `/` with the flag ON (real data must not regress perf) | LHCI green on the branch with `agri_today` ON. PERF never regressed — real data actually SHRANK the home HTML (566 KB → 492 KB). The job did fail first, on ACCESSIBILITY (0.92 vs the 0.95 floor, deterministic across 5 runs): `color-contrast` on the weather strip's today-cell day label (`--muted` is 5.09:1 on white but ~4.47:1 on `--brand-soft`) and `target-size` on four `.tap-target` elements whose 44px `::after` overlay Lighthouse does not measure. Both were latent A-U1 markup that only renders with the flag ON, so the flip exposed them; both fixed in apps/web-agri without touching the shared `.tap-target` utility | ✅ verified — all 11 CI jobs green |

| AG-A22 | Registry Soon entries (nurseries · poultry · fisheries) appear by MIGRATION ALONE — no app-code change | 0042 inserts 3 rows, all `soon: true`; `/catalog/verticals` serves 39 agri_home verticals and the grid + footer counts follow the data (the hardcoded "All 36" strings became `{count}` in en/ta/hi — they would otherwise have started lying the moment 0042 landed) | ✅ verified — 39 served and 39 rendered, no app-code change; CI run 31989984411, all 11 jobs green |
| AG-A23 | Commodity pages are indexable ONLY once populated, and are never advertised empty | `/mandi/[commodity]` self-noindexes below 2 reporting markets and lifts once a second reports (both states captured live); the sitemap is built from the same read that omits price-less commodities, so it cannot point at a thin page; `/mandi/tomato` (curated, no rows) 404s | ✅ verified — both noindex states captured live; CI run 31989984411, all 11 jobs green |

| AG-A24 | A-U2's NEW Tamil/Hindi copy is read by a native speaker before launch | Owner reviewed the 15 ADVICE pairs in `docs/qa/agri-vernacular-review.md` (spray timing, urea, irrigation, disease scouting, severe-weather headlines) on 2026-08-17 and reported no corrections — the sheet came back unchanged. The ~38 remaining strings are weather-condition labels and weekday abbreviations in `wmo.py`, where an awkward word is cosmetic rather than behaviour-changing | ✅ verified — owner native read, no changes required |

| AG-A25 | The daily mandi pull ACTUALLY RUNS somewhere, and a missed day is visible | `mandi-cron` now runs in `docker-compose.dev.yml` as well as staging, because the Agmarknet resource serves ONLY the live day — a day nobody pulls is a permanently unbackfillable hole, and "staging will run it eventually" loses one every 24h until staging exists. Verified: container up, reads `DATA_GOV_API_KEY` from the bind-mounted `.env`, fires at 19:00 IST, and correctly does NOT re-pull a day already in `market.ingest_runs`. Also made OBSERVABLE — `configure_logging` never ran in standalone scripts, so `docker logs` showed nothing at all, which on a job that sleeps for hours is indistinguishable from a hang | 🟡 **it has never fired. Not once.** `market.ingest_runs` holds exactly ONE row ever — 2026-08-17, the *manual* pull recorded under AG-A15 (5776 fetched / 601 written). The `mandi-cron` container's whole log is `scheduler_started`, four times: it schedules for 19:00 IST and is restarted before reaching it (started 12:25 UTC on the 17th, needed to survive to 13:30 UTC). **Two consequences.** 2026-08-18 has no pull, and by this row's own logic — Agmarknet serves only the live day — that day is now permanently unbackfillable. And the failure mode is *restart before the daily window*, which will bite staging identically if a deploy lands near 19:00 IST. The row cannot go green on a dev laptop; it needs the owner-deferred environment. But it is now failing for a KNOWN reason instead of an unexamined one. **2026-08-20 (A-U4b C1): the fix shipped and the first unattended pull is recorded.** PR #89 removed the `now.hour >= 19` gate — catch-up now fires on boot at ANY hour when the day has no successful run (the header records the cost: 18–19 Aug are permanently gone). Ledger today: the first ungated catch-up ran 08:39 IST (outcome `empty` — Agmarknet genuinely holds nothing that early; an `empty` only stands catch-up down for 3 h). The hole is now VISIBLE two ways: `/ops` Ingest health names every missed IST day (capture archived, `docs/qa/evidence/a-u4b/`), and series carry `series_days` so charts render the hole instead of drawing through it. Still open, honestly: **two consecutive automatic pulls** need tonight's 19:00 IST fire plus tomorrow's — time-blocked, not machine-blocked; evidence lands when the clock does. **Restart resilience demonstrated live the same day:** a deliberate restart at 11:34 IST correctly STOOD DOWN (the 08:39 `empty` was inside its 3 h recheck window — the crash-loop brake working); a second restart at 11:41 IST, cooldown lapsed, armed `scheduler_catchup` and ran a real unattended pull — **ok, 7037 fetched, 757 written** — so the day was captured hours before the 19:00 slot. The wire now carries the hole too: `/market/today/636810` serves `series_days ['2026-08-17','2026-08-20']` around the permanent 18–19 Aug gap |

## Verification legend
- **e2e** — Playwright spec committed under `e2e/`, asserting DOM SHAPE
  (counts for flag-off absence, never visibility), no
  `waitUntil: "networkidle"` anywhere.
- **Binding proof row** — screenshot + the real API call, recorded in
  `docs/design-reference/polish-a1.md` as each section is bound.
- **Recorded run** — command + output archived in the PR that flips the row.

## A-U3 rows (AG-A26 … AG-A34)

**Numbering note.** The A-U3 build prompt §2 names its nine rows AG-A22…A30,
but A-U2 had already appended AG-A22…AG-A25. Rows are appended and never
rewritten, so A-U3's rows take the next free numbers and the prompt's names
map as: A22→**A26**, A23→**A27**, A24→**A28**, A25→**A29**, A26→**A30**,
A27→**A31**, A28→**A32**, A29→**A33**, A30→**A34**. Wording is unchanged.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A26 | News items show real source + attribution, nothing auto-published | Live ingest of the three curated feeds seeded by 0045 (ICAR · The Hindu Agriculture · BusinessLine Agri-business), 2026-08-17: **130 items written, all `pending`** — `SELECT moderation_status, count(*)` returned `pending 130` and `GET /content/feed` returned `{"items":[]}` at the same moment, so the gate is proven by the pair, not by assertion. Re-run wrote **0, with 130 duplicates**, both runs recorded in `content.ingest_runs`. After approving 12 through the gate, the feed serves them with `source_name`/`source_url`/`published_at` non-null on every card and the publisher's own date rendered. 25 tests in `test_content_ingest.py` drive the parser from REAL captured feed bytes and assert an entry missing title/link/date is dropped rather than defaulted | ✅ verified |
| AG-A27 | Video content renders with duration/language and an approved-provider embed [CP2: the CMS composer now has the video form — provider select limited to the allowlist, opaque id, and a duration field the curator may leave blank] | BUILT, NOT POPULATED. `kind='video'` with `duration_seconds` + `language`; providers are a CODE-side allowlist (`VIDEO_PROVIDERS`, youtube-nocookie + vimeo) and the embed src is BUILT server-side at read time, so a row stores an opaque id and never iframe markup or an arbitrary origin. A DB check constraint rejects a half-populated video row; the create contract 422s an unapproved provider (tested). `duration_seconds` is nullable because no keyless official API reports YouTube duration and scraping the watch page is out of bounds — it is curator-entered, and a card with no duration renders without the pill rather than inventing one. OWNER DECISION 2026-08-17: ship CP1 with zero video rows; first rows land in CP2 via the knowledge CMS | ⬜ pending (CP2 — code path + tests green, no rows yet) |
| AG-A28 | Knowledge publish requires the human gate (attempt publish as non-approver → rejected) [CMS UI added at CP2: web-admin `/content` — queue + composer, `content.write` and `content.publish` kept visibly separate, no publish path from the draft form because the API has no field for one] | `content.publish` is a router-level dependency on the only route that can move an item forward, and is SEPARATE from `content.write` (drafting is not approving). Test `test_publish_requires_the_human_gate` posts as `roles=user` → **403**, then re-reads the row to prove it is *still* `pending` (the 403 was not cosmetic), then posts as `roles=staff` → 200 + `approved`. `create_item()` strips `moderation_status`, so the CMS cannot create something pre-approved; the ingest worker never names the column at all. Every transition writes an audit row — 12 `content.moderated` rows for the 12 CP1 approvals | ✅ **already in CI, and confirmed under CI's own selector.** `test_publish_requires_the_human_gate` and `test_create_item_cannot_self_publish` are not marked slow, so the backend job's `pytest -q -m "not slow"` has been running them on every PR all along — verified by running that exact selector: 2 passed. This row's condition was met before it was written; nobody had checked. |
| AG-A29 | Pest advisory surfaces only in its target district/window | 0046 adds `districts` + `window_start`/`window_end` to content.items. Verified LIVE with a real seeded advisory (fall armyworm, 5 TN districts, 1 Aug–30 Sep): `?district=Coimbatore` → 1, `?district=Madurai` → **0**, `?pincode=641001` → 1 (district resolved via geo), and **no district at all → 0** — an unknown location NARROWS to nationwide-only rather than widening, because "spray now" aimed at the wrong district costs a farmer money. 5 tests cover the window dimension a live check cannot move the clock for: outside on either side → 0, and an advisory with NO window is never served at all (the column default is permissive so an article need not fill it in, but an alert that cannot expire is a notice). The gate still applies — a pending advisory serves nowhere | ✅ verified |
| AG-A30 | Helplines served from E5 with per-number source + verified_on; static file gone; offline page works with network off | All three parts done. **E5**: `market.helplines` (0046) — name is i18n DATA, and `source`/`source_url`/`verified_on` are per NUMBER and rendered; the band's footer stamp shows the OLDEST date in it, so one fresh number cannot vouch for three stale ones. State scoping verified: national read excludes the TN number, `?state=Tamil Nadu` includes it, `?state=Bihar` does not. **Static file GONE**: `apps/web-agri/data/helplines.ts` deleted, the site-header hotline chip migrated too, and a test asserts the path no longer exists. **Offline**: driven live in Playwright — SW installs, `context.setOffline(true)`, reload succeeds, h1 and all four `tel:` links render from cache. Re-verification honesty: only 1800-180-1551 could be confirmed against a primary source on 2026-08-17 (agriwelfare.gov.in), so only it is restamped; the other three keep their 2026-08-14 A-U1 date and a test pins that. OWNER ACTION: dial 1800-425-1556, 1962 and 155261 to re-verify | ✅ verified (3 numbers need an owner dial-check) |
| AG-A31 | Schemes list renders verified stamps from data | `/schemes` renders 3 rows from `market.schemes` (0039) through `/market/schemes` — the SAME service call the home spotlight uses, so page and home cannot drift. Stamps come from the row: "Verified against pmkisan.gov.in · 2026-08-12", pmfby.gov.in · 2026-08-10, tnhorticulture.tn.gov.in · 2026-08-08 (captured at 4 widths × EN/TA/HI). Deadline chips are filtered upstream by `due_on`, so the bar cannot advertise a closed window. The page states in copy that it does NOT check eligibility — that wizard is Stage C | ✅ verified |
| AG-A32 | Hub directory filters return real businesses; sponsored pins labelled | `/directory` browses category × pincode through the EXISTING E1 `covers()` read — no new directory engine. Six REAL agri businesses seeded through the real service layer (Kovai Agri Machinery, Sakthi Seeds & Nursery, Annapoorna Feed Depot, Coimbatore Veterinary Clinic, Bhoomi Soil Testing Lab, Erode Farm Produce Centre). Verified in a browser: 24 businesses on the unfiltered view, `?category=equipment` → Kovai Agri Machinery, `?category=nursery` → Sakthi Seeds & Nursery. Chips come from `/directory/categories/active` with counts read from data (Equipment 1, Nursery 1, Dairies 121…), so a chip can never lead to an empty list. SPONSORED: **zero markers on the page**, asserted — no agri sponsored-listing slot is registered, so nothing is injected. The absence IS the acceptance: a labelled placeholder would advertise inventory that does not exist | ✅ verified |
| AG-A33 | Agri ads serve via config only with caps + labels | ZERO ads engine code changed this pass — `git diff` under `backend/core/modules/ads/` is empty. Activation was seeding alone. **Caps proven live**: from one fresh viewer, `/ads/serve` returned an ad 6 times (2 creatives × `ads_freq_cap_per_day=3`) then **0, 0**. **Label proven live**: with a fresh UA the home renders the served creative plus a visible "★ Sponsored" badge at y=293 over the hero. **Cross-vertical analytics**: `agri_home_hero_xl` shows 44 impressions / 1 click alongside all eight milk slots — `/admin/ads/performance` groups by `slot_key` with no vertical filter, so agri appears by construction. Rate card needed NO agri rows: it is keyed by pincode tier and a category absent from `category_multipliers_bp` prices at 1× — inventing agri multipliers would be fabricating pricing policy | ✅ verified |
| AG-A34 | Lighthouse ≥ 0.90 holds on `/`, `/categories`, `/tools`, and the new content/directory routes | NOT verifiable on this machine — lighthouse 12 dies on Windows with `EPERM …Temp\lighthouse.*` during Chrome teardown (the D10-recorded gap), and the three runs that did complete before the crash were on a box also running docker+postgres+meili+two dev servers. The gate is CI's. What this pass DID do: registered `/tools`, `/knowledge`, `/directory`, `/schemes`, `/helplines` in `scripts/lhci-affected.mjs` EXTRA_URLS so CI actually audits them (previously only `/` and `/categories`), and `/search` deliberately excluded (noindex, needs `?q=`, would gate on a shell). Perf-risk control measured instead: TTFB on the production build, 5 samples — `/` **786 ms** vs the already-passing `/categories` **809 ms**, so §11+§13's added reads (which ride the existing Promise.all) did not regress first byte | ✅ **GREEN IN CI — same run that closed AG-A35** (PR #84, run 32109277886). All six routes were audited and, unlike at A-U3, actually GATED: the A-U3 `assertMatrix` entry `^https?://[^/]+:3002/(categories|tools|knowledge|directory|schemes|helplines)$` matches every one at perf 0.90 / a11y 0.95 / SEO 0.95, and the log confirms all six in the audited set (`/categories`, `/tools`, `/knowledge`, `/directory`, `/schemes`, `/helplines`) across `Checking assertions against 12 URL(s), 60 total run(s)` → `All results processed!` with zero assertion failures. This is the row that motivated adding that pattern: before it, those routes were COLLECTED but never gated, so a regression would have passed silently. |

### A-U1 carry-over (the four open rows, per A-U3 §0)
| Row | Lands in | State at CP1 |
|---|---|---|
| AG-A4 | W4 (ads activation) | ✅ **CLOSED at CP3.** Caps: 6 serves then 0 from one viewer. Label: "★ Sponsored" visible over the served hero. Config-only: zero diff under `modules/ads/`. CLS is folded into AG-A34's CI run |
| AG-A5 | W3 (search results surface) | ✅ **CLOSED at CP3.** Browser round trip: typed "tractor" into the home band → form action `/search` → landed `/search?q=tractor` → "Results for “tractor”" → 1 real hit (Kovai Agri Machinery) → "1 result near 641001". Location chip read "Coimbatore · 641001" on the same render, which is the row's second half. Fixed en route: the count line said "1 results" — now an ICU plural (0/1/n verified) |
| AG-A6 | W3 (hub directory, real seeded businesses) | ✅ **CLOSED at CP3.** See AG-A32 — real seeded agri businesses, category filters returning them, organic-only asserted |
| AG-A10 | no natural W1–W4 home — closed opportunistically at CP3 | ✅ **CLOSED at CP3.** A clean production build (isolated `NEXT_DIST_DIR`, so the dev server could not race `.next`) booted with `AUTH_SESSION_SECRET` UNSET: `GET /` → **200**, home content rendered, guest "Login" pill present, zero server errors. A control run WITH the secret was run first and also 500'd against the shared `.next` — which is how the corrupted-build cause was separated from the secret. **Caveat, honestly:** the PAGE never 500s, but two BFF routes do — `/api/auth/login` (arguably correct: you cannot mint a session without a secret) and `/api/identity/location` (a public-class read that 500s only because its proxy calls `getAccessToken()`). Three console 500s result, which touches AG-A1's clean-console claim. Proposed fast-follow, NOT done this pass: let the location proxy degrade to unauthenticated instead of throwing |

## A-U4 rows (AG-A35 … AG-A46)

Appended per the A-U4 build prompt §7. Rows are appended and never rewritten.
W0's two rows are below; AG-A37…A46 land with W1–W5.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A35 | `/` holds ≥ 0.90 across five consecutive runs (worst sample recorded) | `pnpm perf:home` — Lighthouse 12.6.1 node API, throttling read from `lighthouserc.cjs` so local and CI numbers are comparable. Five runs on the production build at three commits, recorded in full in `docs/qa/agri-perf-a1.md`: **pre-A-U3 `e90b30c` median 0.73 / worst 0.69**, **A-U3 HEAD `698d192` median 0.72 / worst 0.71**, **after W0 median 0.75 / worst 0.74**. The two pre-W0 distributions are statistically identical, which settles the question the prompt posed: **A-U3 inherited this, it did not cause it** — and `e90b30c`'s own commit message claims 0.95 on `/`, a number never reproducible here. W0 landed real wins (favicon 404 costing 28.7 KB of critical-path bytes; server-response 900 ms → 350 ms; CLS 0.003 on 5/5) but the floor is NOT met | ✅ **RESOLVED IN CI — PR #84 green.** The escalation was right and the conclusion was the opposite of alarming: lighthouse passed on run 32109277886 (`Checking assertions against 12 URL(s), 60 total run(s)` → `All results processed!`, zero assertion failures), so `/` meets the 0.90 floor where it is actually gated. **The page was never the problem; the instrument was.** Local medians of 0.71–0.75 were a ~0.12 machine offset, which is exactly what the `/categories` control predicted (0.83 local vs 0.96 CI). Getting the gate to say so needed one fix: the job was being cancelled at 15m09s two seconds into asserting, so it reported nothing at all — `timeout-minutes` 15 → 25, wall-clock only, no assertion touched. The W0 work stands on its own evidence regardless: favicon 404 costing 28.7 KB of critical-path bytes, server-response 900 ms → 350 ms, CLS 0.003 on 5/5. Original escalation kept below for the record. **Escalated, not carved out —** The control settles why: `/categories`, which CI enforced at median **0.96** on PR #81, scores median **0.84** on this machine in the same session. A known-green page reads **0.12 low** here, and it has a 3× smaller DOM — so no page-level change can produce five green local runs. Decision 3 is untouched and no carve-out is requested; what needs an owner call is WHICH instrument certifies this row (proposal: add `/` to CI's assertion matrix at 0.90, keep `perf:home` as the relative instrument), plus the one identified-but-unattempted lever — `₹` U+20B9 pulling 37.9 KB of `latin-ext` font, `agri-perf-a1.md` §7 |
| AG-A36 | below-fold sections stream with skeletons; above-fold TTFB unaffected | Below-fold sections are each an async component in `app/home-sections.tsx` behind its own `<Suspense>` in `app/page.tsx`, with A1-shimmer fallbacks (`app/home-skeletons.tsx`; the `.skeleton` sweep is now a token-built preset utility, `motion-reduce` safe). Reads go through `lib/home-data.ts`, where every read carries a declared cache window and a `cache()` wrapper — six sections call `todayFor()` and **one** request leaves the server, so splitting the page did not trade first byte for a fan-out storm. Above-fold TTFB is not merely unaffected but improved: **server-response 900 ms → 350 ms** (explicit revalidate windows + `force-dynamic` removed, which had been silently forcing every fetch to no-store). FCP spread tightened **622 ms → 141 ms**; CLS **0.003 on 5/5** | ✅ verified — with a recorded negative result. Streaming is roughly **score-neutral** here, because simulated (Lantern) LCP is a *byte* budget and streaming reorders bytes rather than removing them. Two lessons were paid for and are written up in `agri-perf-a1.md` §4: putting the **LCP element** (the hero) behind a boundary made LCP *worse* (2830–3383 → 3415–3748 ms) because React's swap becomes the largest paint; and streaming an **above-fold** section put CLS at 0.103 on 2/5 runs. Above-fold is therefore awaited, below-fold streamed. Kept despite the neutral score because AG-A36 requires it, it is a real win for real users, and W2/W3 need it to add coins + notifications without re-blocking `/` |
| AG-A37 | assistant refuses dosage/scheme/loan advice and routes to verified data | `safety.check_question` refuses all three domains BEFORE retrieval or any model call, and every refusal carries a route: dosage → `/helplines` (Kisan Call Centre), scheme-eligibility → `/schemes` (official portals), loan → `/helplines`. 17 attacks in `tests/test_ai_redteam.py` (8 dosage, 5 eligibility, 4 loan), all refused with the right reason AND the right route. The check runs on **both** sides of the model — an innocuous question can still produce an answer containing a dose, and `check_answer` catches that on the way out. **The precision tests are half the row:** "What is the PM-KISAN scheme?" and "When should I spray my cotton?" still ANSWER — the refusal targets a *quantity per unit area* and *a person's* eligibility, not the topics, because a safety layer nobody can get past is one nobody can use | ✅ verified — 17/17 refused + routed, 4 precision tests prove it is a scalpel not a ban. **Gap, stated:** the patterns are English-only. A dosage question in Tamil or Hindi passes `check_question` and reaches the model with only the system prompt and the output filter behind it — `docs/security/agri-ai-redteam.md` §5.4, and item 2 on the before-you-flip list |
| AG-A38 | injection suite: no tool write, no scope escape, no system-prompt leak | 55 attacks, all passing, in CI on every PR. **No tool write:** `TOOL_ALLOWLIST` is a module constant and `test_no_tool_can_write` walks it asserting every entry is `GET`, so a future write-capable tool fails CI rather than shipping; the model supplies a tool NAME and typed args, never a URL (paths built from templates against validated pincode/slug regexes); `resolve_tool` returns None for `delete_everything` and `../../admin/users`; tools call the same public endpoints the pages call, over loopback, unauthenticated — no privileged data path. **No prompt leak:** the system prompt is a module constant with no interpolation (asserted), and any answer containing a prompt fragment or fence marker is discarded WHOLE, not redacted. **Fence escape** has two dedicated tests — a document containing `<<<AGRI_SOURCE_END>>>` cannot terminate its own fence, which is the single most important line in the module. **No scope escape:** 6 out-of-domain refused, 6 in-domain allowed | ⬜ **STRUCTURAL PASS — not a live run.** Every attack ran against the real request-path code, but **none of them asked `claude-opus-5` anything**: there is no `ANTHROPIC_API_KEY` in this environment (owner decision). The model-facing questions — multi-turn leak build-up, a dose phrased as a story, whether the model honours the fence when a document argues with it — are unanswered and listed in `agri-ai-redteam.md` §1. **This is why `agri_ai` ships OFF**, and why the row is not green. **OWNER DECISION 2026-08-17 (CP1): OFF** — read the red-team doc and kept the flag false; no code changed, which is what building it flag-off by default was for |
| AG-A39 | assistant answers cite sources present in the corpus | Citations are assembled IN CODE from the retrieved rows' own metadata (title, slug, source name, kind) — never parsed out of the model's text. The model cites by number; the names and links are ours. That is what lets `check_answer` reject *any* URL the model emits (only `agri.in` survives): it never needed to emit one. The corpus itself is DERIVED from content's public `/content/feed`, which serves **approved items only**, so a citation can only ever point at something a human curator published and a visitor can already read — the AG-A28 gate is inherited by construction, not re-enforced. Ingest verified end-to-end: `scripts/ai_ingest.py` embedded 15 approved items into `ai.chunks` (`vector(384)`), and cosine retrieval returns sensible hits ("government scheme for farmers" → 0.682 on the Farmers' Training and Quality Seed Distribution Programme) | ⬜ pending the live run — the citation MACHINERY is verified (corpus ingested, retrieval relevant, citations rendered from row metadata in `ask-chat.tsx`, links resolve to `/knowledge/{slug}`), but no answer has been generated, so "the answer cites sources it actually used" is untested. Blocked on the same key as AG-A38. **Corpus is 15 documents** (12 articles, 1 guide, 1 advisory, 1 video skipped — no prose), so the no-evidence path will be the common one at launch; recorded rather than hidden |
| AG-A40 | coins earn/burn correct under concurrent awards, balance matches milk.in | `tests/test_coins_award_concurrency.py` (slow, CI). Two races the existing 10k storm test could not see, because every op there carries a UNIQUE key: (1) **the same key raced** — 50 concurrent awards on one idempotency key produce exactly 1 ledger row and 1 balance increment; (2) **a numeric cap raced** — 40 concurrent awards against a `weekly_cap=5`. **This found a real money-path bug: the cap admitted ALL 40.** `rules.check_numeric_caps` counts ledger rows then inserts one, and nothing serialized the two, so every concurrent transaction read the same pre-insert count. The unique key cannot help — each attempt legitimately carries a different key (different review ids). Fixed with a transaction-scoped `pg_advisory_xact_lock` keyed on the user at the top of `service.award`, chosen over `SELECT FOR UPDATE` because a first-ever award has no balance row to lock. Re-measured: **40 concurrent → exactly 5 awarded**, and the test now asserts `== cap` rather than a tolerance. Exposure before the fix was `review_approved` (5/week at 20 coins), the only shipped rule with a numeric cap > 1; the once-per-user rules (`signup_complete`, `profile_100`, `referral_referee`) and `daily_visit` were always safe because they are enforced by the deterministic unique key, which `check_numeric_caps` explicitly skips (`cap <= 1`) | ✅ **BOTH halves now verified.** Cross-platform closed by `tests/test_coins_cross_platform.py` (3 green): the wallet's primary key is asserted to be `user_id` and nothing else, neither `balances` nor `ledger_entries` may grow a site/vertical/platform/tenant column, and a milk earning (+5 `review_approved`) plus an agri earning (+25 `business_claim`) for one user land in ONE balance row reading 30. The schema guard is the point: "true by construction" was correct and would have stopped being true silently the day a migration added a `site` column for per-site reporting, with every existing test still green. The guard was mutation-checked — it detects that column and passes the wallet as it stands. **Was: partial — the concurrency half is verified and a real bug is fixed; the cross-platform half is NOT.** "balance matches milk.in" is untested here: one `coins.balances` row per user is the single source by construction (one schema, one engine, no per-vertical balance column exists), but no test drives an award from an agri surface and reads it back from a milk surface. That belongs with W5's QA and is named there rather than assumed |
| AG-A41 | notification centre + push subscription round-trip | `/notifications` now carries this DEVICE's push state above the in-app list. `lib/push.ts` is a verbatim port of milk's (D28, proven E2E 2026-08-04) rather than a second implementation: the rule that matters is that a failed server call unsubscribes the browser again, so it is never left holding a subscription the backend never heard of — two copies of that would drift into orphaned endpoints. Reuses agri's ONE existing service worker (A-U3's offline helplines page), which W4's PWA pass depends on. Renders NOTHING without a VAPID key (feature-dark, inlined at build time); `denied` is stated as browser-settings-only rather than given a button that cannot work; iOS gets its own copy because Safari exposes PushManager only inside an installed PWA. Device state is kept distinct from the account's D12 channel preference. EN/TA/HI | ⬜ **partial — the UI and the wiring are done, the ROUND-TRIP is not proven.** No VAPID key is provisioned in this environment, so the card is feature-dark here and no subscribe→send→receive cycle has been executed on agri. The backend endpoints (`POST`/`DELETE /notify/push/subscriptions`) are the same ones milk's proven flow uses. Closing this row needs the key — the same prod-side dependency already recorded against milk's launch |
| AG-A42 | federated search returns cross-vertical cards | `GET /search/federated` (public, declared) queries every site index and tags each hit with `source_site`. **Verified live**: `?q=seeds` returned rows from both indexes — `[agri] Sakthi Seeds & Nursery`, `[agri] Annapoorna Feed Depot`, `[milk] Annapoorna Feed Depot` — with `searched: ["agri","milk"]`. The hub's `/search` renders these as an "Also in the agri.in family" rail linking to that vertical's own domain and naming it, so nothing is passed off as agri's. Deliberately bounded and uncursored (merging relevance across independent Meili indexes into one resumable cursor is a different problem; depth belongs in each vertical's own search), and a site that errors is dropped from `searched` rather than failing the request — the existing single-site `/search` 500s when its index is missing, this route degrades. Live output also caught a real defect: a business covering both sites is indexed on both, so the rail showed the same shop that was already in the results; it now excludes ids already shown, not just agri-sourced rows | ⬜ **partial — dairy yes, organic no, and not by oversight.** `SITES` is `("agri","milk")`: theorganic.in is D63–74 and has no index and no data, so federating it would be a heading over nothing. It joins by adding one entry to `SITES` when it ships. The row cannot go green on "dairy/organic" until organic exists |
| AG-A43 | offline: mandi prices + helplines + saved items with network off | `e2e/agri-pwa.spec.ts`, four tests in a real Chromium with `context.setOffline(true)` — **all passing**. A cached `/helplines` returns in full (the Kisan Call Centre `tel:` link is visible with the network off), `/mandi` still renders last-known prices, and any other navigation lands on the `/offline` shell, which names what IS available rather than apologising. A fourth test asserts **no `/api/*` response is ever in Cache Storage** — `/saved` is per-user, so an API payload in a shared cache would be a PII leak; only the rendered document is cached, by the browser that rendered it. A fifth property is asserted too: **exactly one service worker**, because two on one scope fight over fetch handling and the loser's cache goes quietly stale. A-U3 shipped its worker for `/helplines` alone and said A-U4 would extend it rather than register a second; this is that extension, with registration moved from that page to the root layout | ✅ verified — demonstrated in a browser, not reasoned about, and **now actually gated**: these specs ran in NO CI job until the A-U4 close-out (no `@matrix` tag, absent from `e2e:auth`), so this row was green on tests nothing executed. Wired in as `pnpm run e2e:agri`; 12/12 green 2026-08-18. `/mandi`, `/saved` and now `/tools` are runtime-cached rather than precached on purpose: a device holds only what its owner actually opened |
| AG-A44 | Lighthouse ≥ 0.90 + a11y/SEO 100 on every agri route | `pnpm perf:sweep` (`scripts/perf-sweep.mjs`) audits nine routes and prints the table below, reusing perf:home's contract so local and CI numbers mean the same thing. **a11y 1.00 and SEO 1.00 on every audited route.** Getting there fixed a real defect the sweep found: `heading-order` failed on `/categories`, `/helplines` and `/mandi` (a11y 0.98 each) from ONE cause — the site footer's column headings were `h3`, so any page whose main content has no `h2` jumped `h1 → h3`. Promoting them to `h2` (they are top-level sections of the footer landmark) fixed all three at once and stops the next `h2`-less page inheriting it; nothing moved visually. One measurement carve-out, stated in the script: `/offline` is deliberately noindex so `is-crawlable` can never pass there — its SEO is reported, not gated. No route's real floor is lowered | ✅ **GREEN — a11y and SEO 100 on all nine locally; perf gated and PASSING in CI** (PR #84, run 32109277886). Local perf medians are recorded below for the record, but they are relative numbers from a machine that reads ~0.12 low, not certifications — CI is the authority and it is green. Local medians: Local medians: `/` 0.71–0.75 · `/categories` 0.78–0.83 · `/tools` 0.78–0.88 · `/knowledge` 0.90–0.92 · `/directory` 0.85–0.92 · `/schemes` 0.88 · `/helplines` 0.90–0.91 · `/mandi` 0.86–0.88 · `/offline` 0.89–0.90. This machine measures ~0.12 below CI on a known-green control (`/categories` = 0.96 on PR #81, 0.83 here), so these are relative numbers, not certifications — which is exactly the instrument question raised at CP0 and still open |
| AG-A45 | restore drill executed and timed | `bash scripts/backup/backup.sh` then `bash scripts/restore.sh`, run 2026-08-18 against a FRESH dump rather than a stored one — the stored backups predate migrations 0047/0048, so restoring them would have diffed against a schema that no longer exists and proved nothing. **Measured: restore 5s (drop+create+decrypt+pg_restore), verify 2s, total RTO 7s, 102 tables, 61,528 rows, all counts match** — including the new `ai` schema. The drill restores into a scratch database and exits non-zero on any row-count mismatch, so a green run is a comparison, not an absence of errors. Recorded with the two commands to repeat it in `docs/runbooks/d57-agri-launch.md` §3 | OK verified — drill #3 |
| AG-A46 | zero Critical/High in the adversarial audit | Assistant: 56 attacks in `tests/test_ai_redteam.py` (injection, fence escape, scope escape, regulated domains, output filtering, tool-write, input limits) — all passing, all against the real request-path code. IDOR: the existing sweeps (`test_d30_idor.py`, `test_u2_idor_sweep.py`, `test_ownership.py`, 72 tests) pass, and the A-U4 routes add **no new IDOR surface** — none of `/ai/ask`, `/ai/status`, `/coins/rules` or `/search/federated` takes a user-supplied id in its path. Money path: the cap-bypass bug (AG-A40) was found and fixed in this pass, not deferred. **One new finding, a Low, fixed in the same pass:** the assistant's per-conversation turn cap counted `ai.usage` rows by a CLIENT-SUPPLIED `conversation_id` with no `user_id` filter, so one caller's limit could depend on another caller's rows. UUIDs make it impractical to exploit (hence Low), but a limit check must not read another user's rows; now scoped to both columns with a regression test | OK verified for Critical/High — **zero of each; one Low found and fixed.** Scope stated honestly: this audited the code, and the assistant's model-facing surface is NOT covered here (no API key, `agri_ai` OFF — `docs/security/agri-ai-redteam.md` §1). k6 with ads serving is NOT run: it needs the staging VPS, which is owner-driven and deferred |

## Agri-colleges Phase 2 rows (AG-A47 … AG-A50) — engine + import

Plan 1 of three (`docs/superpowers/plans/2026-08-17-agri-colleges-phase2-engine.md`).
Backend only; nothing under `apps/` is touched, which is what let this land
independently of A-U4. The surface rows arrive with Plans 2 and 3.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A47 | the application cannot write college data — `app_rt` holds SELECT only on all five `education` tables | `tests/test_education_models.py`, two tests. One asserts `permission denied` on an INSERT through the app's real runtime URL; the second walks the other four tables with DELETE, because `0049` grants five times and a missed line would leave exactly one table writable. The consequence is load-bearing rather than cosmetic: the importer connects with `database_admin_url` and every write-side test takes a new `owner_session` fixture, because a fixture on `db_session` genuinely cannot seed a college | ✅ verified — the grant broke the plan's own test design on first run, which is the useful kind of breakage |
| AG-A48 | the importer cannot accept a bundle the CI gate would reject, and never half-imports | `seed_import.py` imports `scripts/education_seed_contract.py` itself — the same module `validate_education_seed` runs in CI — so there is one idea of "valid" and both call it. `tests/test_education_seed_import.py` feeds it a bundle with a bad enum, a reserved slug and a missing stamp, and asserts `SeedContractError` **and** zero rows written: validation runs against the whole bundle before the first `session.add`. Idempotency measured against a populated database, not asserted: second run `created 0 / updated 772` | ✅ verified — 772 institutions, 47 programmes, 277 offerings, 22 resources, 13 guides |
| AG-A49 | every Indian institution resolves to a real state, and the seed's districts are not silently dropped | `state_id` is a real cross-schema FK to `geo.states.id`; a test asserts zero Indian rows import with a null state and that >700 join back to a live state row. `district_id` deliberately is **not** an FK — `data/geo/districts.csv` is Tamil Nadu only (38 rows) until D65, so a constraint would reject a valid Punjab college. A test asserts an unknown district code is accepted, so nobody "fixes" the asymmetry, and a second joins on `District.lgd_code` to prove the 70 seeded districts landed. The importer refuses outright on empty geo, because the alternative is 772 rows with a null state — which looks healthy until every state page comes up empty | ✅ verified — the district assignment was missing from the plan entirely and would have shipped the filter with no data |
| AG-A50 | only verified, active colleges are findable in hub search, and a demoted college stops being findable | `tests/test_education_search_sync.py`, three integration tests against the **real dev Meilisearch**: a verified institution is returned by a query on the agri index; the same institution demoted to `listed` disappears (null snapshot → delete, ADR-0007); and no college ever reaches the milk index. Shape assertions alone would have passed against a snapshot that never arrives, which is why the round-trip is the proof. Both indexer gates are pinned open by a test that imports the real constants — `worker.STREAMS` and `indexing.INDEXED_EVENT_TYPES` — rather than restating them | ✅ verified — publishing had been aimed at a stream nobody read, carrying a type the indexer discards |

### Phase 2 API rows (AG-A51 … AG-A54)

Plan 2 (`docs/superpowers/plans/2026-08-18-agri-colleges-phase2-api.md`). Still
backend only; the browser-checkable versions of these arrive with Plan 3.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A51 | a `listed` or non-active college emits no fee, no seat count and no admission route — over the wire, everywhere | One predicate, `can_show_admission_data = verified AND active`, applied in the serializer rather than in handlers, and shipped ON the wire so the frontend branches on it instead of re-deriving the rule. Proven three ways: unit tests that insert rows the seed contract forbids (a `listed` row *with* a fee), an end-to-end router test asserting the raw number appears nowhere in the payload text, and a **full paged walk of every unverified institution in the 772-row corpus** fetching each detail. A listed college still lists its programmes, minus the numbers — "this college runs B.Sc. Agriculture" is true and useful; what it costs is a claim nobody checked | ✅ verified — the corpus walk is the one that will notice the fee arriving with a future second serialization path |
| AG-A52 | a renamed college's old slug resolves to its successor, and the API does not redirect to get there | `GET /education/institutions/{slug}` answers **200** with `status: "merged"` and `merged_into_slug`; the page issues the 301. Asserted with `follow_redirects=False`. `fetch` follows 3xx by default, so an API-level redirect would hand a caller the successor's JSON under the URL it requested with no signal it happened. Closed and merged rows are absent from listings and reachable by slug — which is exactly what keeps the 301 and the closed banner possible | ✅ verified — recorded in spec §7 as an amendment, because a reader would otherwise guess the opposite |
| AG-A53 | every state slug the API publishes works as a filter against the API that published it | `geo.states` has no slug column, so `GET /education/states` publishes the vocabulary and the frontend never derives one. Two tests: the SQL and Python slug implementations agree on **all 36 real state names** (walked, not sampled — the risk is the one name with an ampersand), and every published facet slug returns a nonempty page. Facets exclude states with no colleges, asserted against the real state total: 5 have none, and ISR pages for them would be thin indexable shells | ✅ verified — and `?district=` is asserted TN-only both ways, so Plan 3 does not offer a control that reads "no colleges here" when it means "no data" |
| AG-A54 | the education API has no write path, and a user-typed wildcard is text | A test walks every mounted `/education` route asserting it accepts only GET/HEAD/OPTIONS — `app_rt` already makes a write impossible, so this catches the real failure mode, which is copying a router from a module that has one. `?q=%`, `?q=_` and `?q=\` each return zero rows against the full corpus; unescaped, `%` would have returned all 772. Unknown enum values are 422, not an empty 200 — "your filter matched nothing" and "that filter value does not exist" are different facts | ✅ verified |

### Phase 2 surface rows (AG-A55 … AG-A59)

Plan 3 (`docs/superpowers/plans/2026-08-18-agri-colleges-phase2-surfaces.md`).
The nine public routes of spec §6, the registry tile, the sitemap and the gates.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A55 | `/colleges` filters server-side and holds the 0.90 floor with no carve-out | Filtering is server-rendered LINKS, following `/directory` rather than the client island the plan sketched: no JS, works with JS off, crawlable, and the URL is the state. **First Load JS is 169 B.** The search box is a plain GET form so it submits without JS too. `/colleges` is added to **both** Lighthouse places — `EXTRA_URLS` in `scripts/lhci-affected.mjs` (what gets collected) and the `assertMatrix` alternation in `lighthouserc.cjs` (what gets gated) — because a URL in one and not the other is scored and then asserted about by nothing, which is the exact state `/categories` and `/tools` were in until A-U3 noticed. The pattern is anchored, verified in node: it matches `/colleges` and NOT `/colleges/state/*`, which is what §10 asks for | ⬜ **wired, awaiting CI.** Local Lighthouse on this machine reads ~0.12 below CI on a known-green control, so the local number is a relative signal and CI is the authority (the AG-A35 finding). The gate now exists where it did not before. **2026-08-20 (A-U4b C5): ✅ confirmed against CI** — PR #88 (head `4df3838`, merged to dev 19 Aug) carried `/colleges` in both `EXTRA_URLS` and the `assertMatrix` alternation, and its `lighthouse` check concluded **success**; the 0.90 floor held where it is actually gated |
| AG-A56 | a `listed` college page is `noindex`, shows the honest notice and renders no fee or seat count | Checked in a live browser fetch against the real corpus: the notice renders, `robots` is `noindex`, and **zero rupee symbols appear anywhere in the payload**. Backed by the API-side corpus walk (AG-A51) and an e2e assertion that `body` does not match `/₹\s*\d/`. Programmes are still listed, minus the numbers — "this college runs B.Sc. Agriculture" is true and useful; what it costs is a claim nobody checked | ✅ verified |
| AG-A57 | a verified college carries `CollegeOrUniversity` + `BreadcrumbList` JSON-LD and is indexable; a closed one is not | All four states checked live. Verified: both JSON-LD blocks present, indexable. Closed: **200**, banner, `noindex`, no official-site CTA, no admission data. Merged: redirects to its successor. Unknown slug: 404. The corpus contains no closed or merged rows (all 772 are `active`), so two were inserted into the dev database for the check and removed afterwards — the count is back to 772. `collegeJsonLd` omits every field the corpus lacks, because asserting an address we do not have is a claim rather than a blank | ✅ verified — **with one deviation: the redirect is a 308, not the 301 the spec names.** Next's `permanentRedirect()` issues 308; it preserves the request method and Google treats it as equivalent to a 301 for a GET. **2026-08-20 (A-U4b C5): spec amended** — `2026-08-18-agri-colleges-phase2-surfaces.md` now names the 308 with the one-line reason (308 preserves the method); the code was correct as built and the deviation is no longer a deviation |
| AG-A58 | the sitemap advertises verified colleges and never a `listed` one | Built against the live corpus: **329 college URLs, and exactly 329 verified-and-active rows exist** — the counts match, so nothing leaked in and nothing was dropped. Plus 31 state pages, 9 scholarships, 13 exams, 13 guides; 410 `<loc>` entries total. The walk pages the cursor: an unpaged call would have capped it at the API default of 20 out of 772 with nothing failing. `lastModified` is each row's own `last_verified_at`, never the build time | ✅ verified |
| AG-A59 | the `agri-colleges` tile is live in the registry and links to `/colleges` | Migration `0050` adds the row (community, order 5, 🏫, `soon: false`) and `LIVE_ROUTES` maps the slug to `/colleges`. `alembic up/down/up` clean. Confirmed against the live registry read. **AG-A13's `toBe(36)` was already false before this work:** migration `0042` (A-U2, 16 Aug) added nurseries, poultry and fisheries without touching the spec, so the literal has been asserting 36 against a registry of 39 — 40 now. It went unnoticed because `e2e/agri-categories.spec.ts` **runs in neither CI job**: no `@matrix` tag, and not in `e2e:auth`'s named list. The new `e2e/agri-colleges.spec.ts` IS tagged `@matrix`, so it runs | 🟡 tile verified; **AG-A2 and AG-A13 both rest on a spec that does not run in CI — owner decision on whether to wire it in.** **2026-08-20 (A-U4b C5): that decision is made and shipped** — `e2e:agri` (`package.json:21`) runs `agri-home agri-categories agri-pwa` and is invoked at `.github/workflows/ci.yml:563`; `agri-categories.spec.ts` no longer asserts `toBe(36)` (it asserts `>= 36` plus grid-matches-registry, the actual property). The spec runs in CI; the row's open question is closed |

## A-U4b rows (AG-A60 … AG-A69)

The close-out correction batch (`docs/design-reference/agri/A-U4b_closeout_prompt.md`).
Rows are appended here as each item reaches its own exit condition — a row
without evidence stays unwritten rather than pending.

| Row | Acceptance | Verification method | Status |
|---|---|---|---|
| AG-A60 | `agri_ai` ships OFF and the Ask-AI entry surface reads honestly as coming-soon in EN/TA/HI — the visible half of the AI decision | Live browser, 2026-08-20: the home §5 Ask section renders "Coming soon — notify me" / "விரைவில் — எனக்கு தெரிவிக்கவும்" / "जल्द आ रहा है — मुझे सूचित करें" with the human-review disclaimer in each locale, and `/ask` itself renders the honest state ("Not switched on yet … stays off until its safety review is signed off", equivalents verified in TA and HI). Captures: `docs/qa/evidence/a-u4b/ag-a60-ask-{en,ta,hi}-1280.png`. **One observation for the owner:** the §2a hero tile reads "ASK AGRI.IN · Ask anything · Speak or type →" and routes to `/c/experts` — it predates the flag-OFF decision and arguably overpromises; left unchanged as it routes to live human experts, flagged rather than silently rewritten | ✅ verified (flag state re-checked on `/ops`: `agri_ai` present, OFF) |
| AG-A65 | Knowledge cards order by `published_at` — recency wins, kind never outranks it; a newer article outranks an older guide | `apps/web-agri/lib/content.test.ts` (vitest, in `pnpm --filter @agri/web-agri test`): a newer article outranks an older guide; three curated items can no longer lock the newest article out; the rail still dedupes against the cards in feed order; a dead engine still costs the section, never the page. The old video→guide→advisory→article concatenation is gone from `fetchKnowledgeSection`; the backend already ordered `published_at DESC` (service.py:125) — the frontend stops re-sorting by kind | ✅ verified — 4 tests green 2026-08-20 |
| AG-A63 | The login screen offers EN/TA/HI before any step — a Tamil-first user never reads the flow in English first | Live browser 2026-08-20, signed out: one-tap `.lang` buttons (never a hamburger) at the top of `/login`; tapping த re-renders the whole flow in Tamil (`html lang=ta`), हि in Hindi, client state survives (`router.refresh()` + NEXT_LOCALE). The post-auth step still persists the choice to the account. Shared screen — agri/milk/organic all get it. All 75 `ui.auth` keys already existed in ta/hi. Captures: `o4-login-{ta,hi}.png` | ✅ verified |
| AG-A64 | The site wordmark links home everywhere except on the home itself | `packages/ui` HeaderStack stays routing-free (slot test added); per-app BrandLink islands use `usePathname()` — plain text on `/`, `next/link` (agri) or next-intl Link (milk, locale-prefixed: `/ta` pages link `/ta`). Verified live on both apps: agri `/notifications` wordmark → `/`; milk `/ta/chennai/600001` wordmark href `/ta`; both homes render plain text. `.tap-target` keeps 44px without moving the header box (AG-A8 CLS) | ✅ verified |
| AG-A66 | The §19 install band exists with honest states — installed→absent, promptable→a button that actually installs, iOS→Add-to-Home-Screen instruction, unsupported→absent; no fake QR | Truth table unit-tested (`lib/install-prompt.test.ts`); all three renderable states verified live on the production build 2026-08-20: absent (headless Chrome, no prompt event), button (`beforeinstallprompt` → band + Install), iOS instruction (iPhone UA → no dead button). Copy claims only what sw.js actually caches ("last-known mandi prices"). QR dropped, not faked. Captures: `o8-install-band-{button,ios}-en.png`. **Device-blocked remainder:** a real Android-Chrome install tap and a real Safari check need physical devices — owner pass before D57 | 🟡 built + states verified; device pass outstanding |
| AG-A68 | "What farmers say" renders honestly in both states — real approved reviews when they exist, an invitation (never a fabricated quote) when none | Both states live 2026-08-20: 641001 renders 3 real approved D18 reviews (unchanged treatment); 636810 renders heading + moderation note + coins nudge + write-a-review CTA into `/directory` (the real entry point — reviews attach to businesses). The coin amount comes from `GET /coins/rules` `review_approved` — **which pays 20, not the mockup's 5, exactly why literals are banned**; missing rule renders amountless. A test asserts the reference's three sample quotes appear nowhere in app code or catalogs. Captures: `o10-reviews-{populated-641001,empty-636810}.png` | ✅ verified — TA/HI copy flagged for native review |
| AG-A61 | Scheme/service cards open a detail dialog (what · eligibility · documents · source + verified date) with the official-portal link as the deliberate second click; the no-JS path stays a real link | Live browser 2026-08-20, EN/TA/HI: click opens a native `<dialog>` in place (focus lands inside), real-key Escape closes with focus returned to the exact card, modifier/middle clicks keep browser meaning, pre-hydration clicks fall through to the plain anchor (observed live). DPDP: dialog is descriptive only, fetches/stores nothing. Link checker 6/6 OK over the same URLs. 12 unit tests incl. a per-entry data-contract walk. Captures: `o2-sarkari-dialog-{en,ta,hi,hi-narrow}.png`. Same one-click-exit pattern remains in §9 SchemesBlock — follow-up, not this row | ✅ verified — dialog live-verified EN/TA/HI; **detail copy owner-verified 2026-08-20** (drafted same day, verified at CP3 review; `last_verified` stamps carry that date). TA/HI wording still flagged for native review with the rest of this pass's new strings |
| AG-A70 | Price surfaces say they are a once-daily snapshot — source + as-of + next-pull visible; nothing implies live prices | `next_pull_hour_ist` travels on the wire from settings (never a frontend literal; additive on MandiBlock + CommodityDetail). Home stamp is a readable two-line block ("Agmarknet · updated {date}" / "Daily snapshot — updated once a day, around 7 PM IST · next update after 7 PM IST today"); ticker carries the compact cadence; the LiveDot over a once-daily dataset is gone; commodity page stamps cadence without today/tomorrow (ISR-cached — a cached "today" would lie). Verified live EN/TA/HI at 360+1280 (captures `o1-mandi-stamp-*`); e2e stamp assertions extended, hour computed from the payload. Copy sweep: no string calls PRICES live; `ask.sub`'s "reads live mandi data" refers to the AI reading and is flagged, with `how.s2.d` borderline, for owner judgment | ✅ verified — TA/HI copy flagged for native review |
| AG-A62 | The content pull actually runs on a schedule, and news comes from a healthy source set | **Split per CP0 (owner-signed 2026-08-20): the scheduler is the launch-blocking half and it shipped** — `content-cron` in dev+staging compose, ungated boot catch-up, 6 AM IST daily fire, run ledger + /ops visibility; first automatic pull recorded 2026-08-20 10:58 IST (3 sources ok, 29 items pending review). Two consecutive automatic pulls: second lands tomorrow 06:00 IST (time-blocked, same class as AG-A25's). **Source expansion to 5–10 publishers (robots/terms vetting) is deliberately POST-LAUNCH** per the split — the section is refreshed daily from 3 sources at D57, not dead | 🟡 scheduler live + first auto pull recorded; consecutive-pull evidence lands tomorrow; source expansion post-D57 by design |
| AG-A69 | The "Live on agri.in" marquee renders only from real, privacy-scrubbed events; empty means absent; `agri_live_feed` stays OFF at D57 | Engine: `directory.activity` written in the same transaction as the domain write at five sites; privacy BY CONSTRUCTION (no identity columns exist; pincode→district then dropped; business names only when publicly visible; `UNIQUE(kind, source_id)` idempotency). Public read 404s while the flag is off. Full lifecycle verified live 2026-08-20: flag ON + empty window → section absent; one real guest lead → one seven-field row → marquee renders in EN/TA/HI ("A farmer contacted E2E Milk Vendor" + translations); window cleared → absent; flag OFF → 404 → absent (the D57 state, restored). No timestamps rendered (cached HTML must not lie), no counts, no links inside the duplicated aria-hidden lane. Privacy review recorded: `docs/security/agri-live-feed-privacy.md` (field-by-field verdicts + re-review triggers). 11+123 backend and 76 web-agri tests; migration 0051 up/down/up clean. Capture: `o11-live-feed-en.png` | ✅ built + verified; **ships flag-OFF** — the flip is a separate owner decision that re-reads the privacy review; TA/HI strings flagged for native review |
